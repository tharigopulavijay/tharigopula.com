# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# This project uses bun.lock as its lockfile (see bunfig.toml). package-lock.json
# is only generated when running npm locally — committing it would make CI/Lovable
# builds ambiguous about which package manager to use.
package-lock.json

# Local agent tooling — launch.json holds machine-specific absolute paths
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
