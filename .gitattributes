# Normalise line endings. Windows checkouts get CRLF locally, but everything is
# stored and built as LF so Cloudflare's Linux builds match local exactly.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
